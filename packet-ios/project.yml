name: Packet
options:
  bundleIdPrefix: com.resolo.packet
schemes:
  Packet:
    build:
      targets:
        Packet: all
    run:
      config: Debug
    test:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
targets:
  Packet:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - Packet
      - SharedTunnel
    resources:
      - Packet/PrivacyInfo.xcprivacy
    dependencies:
      - target: PacketTunnel
        embed: true
    settings:
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      CURRENT_PROJECT_VERSION: 1
      CODE_SIGN_STYLE: Automatic
      CODE_SIGN_IDENTITY: "Apple Development"
      CODE_SIGN_ENTITLEMENTS: Packet/Packet.entitlements
      DEVELOPMENT_TEAM: BXY4DH6H7D
      GENERATE_INFOPLIST_FILE: YES
      INFOPLIST_KEY_CFBundleDisplayName: Packet
      INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: YES
      INFOPLIST_KEY_UILaunchScreen_Generation: YES
      MARKETING_VERSION: 1.0.0
      PRODUCT_BUNDLE_IDENTIFIER: com.resolo.packet
      PRODUCT_NAME: Packet
      SWIFT_VERSION: 5.0
      SWIFT_OBJC_BRIDGING_HEADER: Packet/Packet-Bridging-Header.h
      "OTHER_LDFLAGS[sdk=iphoneos*][arch=arm64]":
        - "$(SRCROOT)/../target/aarch64-apple-ios/release/libphantom_client.a"
        - "-lresolv"
      "OTHER_LDFLAGS[sdk=iphonesimulator*][arch=arm64]":
        - "$(SRCROOT)/../target/aarch64-apple-ios-sim/release/libphantom_client.a"
        - "-lresolv"
      "OTHER_LDFLAGS[sdk=iphonesimulator*][arch=x86_64]":
        - "$(SRCROOT)/../target/x86_64-apple-ios/release/libphantom_client.a"
        - "-lresolv"
      ENABLE_BITCODE: NO
  PacketTunnel:
    type: app-extension
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - PacketTunnel
      - SharedTunnel
    resources:
      - PacketTunnel/PrivacyInfo.xcprivacy
    settings:
      APPLICATION_EXTENSION_API_ONLY: YES
      CURRENT_PROJECT_VERSION: 1
      CODE_SIGN_STYLE: Automatic
      CODE_SIGN_IDENTITY: "Apple Development"
      CODE_SIGN_ENTITLEMENTS: PacketTunnel/PacketTunnel.entitlements
      DEVELOPMENT_TEAM: BXY4DH6H7D
      GENERATE_INFOPLIST_FILE: NO
      INFOPLIST_FILE: PacketTunnel/Info.plist
      MARKETING_VERSION: 1.0.0
      PRODUCT_BUNDLE_IDENTIFIER: com.resolo.packet.PacketTunnel
      PRODUCT_NAME: PacketTunnel
      SKIP_INSTALL: YES
      SWIFT_VERSION: 5.0
      SWIFT_OBJC_BRIDGING_HEADER: Packet/Packet-Bridging-Header.h
      "OTHER_LDFLAGS[sdk=iphoneos*][arch=arm64]":
        - "$(SRCROOT)/../target/aarch64-apple-ios/release/libphantom_client.a"
        - "-lresolv"
      "OTHER_LDFLAGS[sdk=iphonesimulator*][arch=arm64]":
        - "$(SRCROOT)/../target/aarch64-apple-ios-sim/release/libphantom_client.a"
        - "-lresolv"
      "OTHER_LDFLAGS[sdk=iphonesimulator*][arch=x86_64]":
        - "$(SRCROOT)/../target/x86_64-apple-ios/release/libphantom_client.a"
        - "-lresolv"
      ENABLE_BITCODE: NO
