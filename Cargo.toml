[workspace]
members = [
    "packet-proto",
    "packet-server",
    "packet-client",
    "packet-bridge",
    "packet-relay",
]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"

[patch.crates-io]
tun2socks = { path = "vendor/tun2socks" }
